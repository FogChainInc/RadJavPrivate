# Read this!

The files within this directory are used to generate `lib.d.jump` and `lib.es6.d.jump`.

## Generated files

Any files ending in `.generated.d.jump` aren't mean to be edited by hand.
If you need to make changes to such files, make a change to the input files for our [library generator](https://github.com/Microsoft/TSJS-lib-generator).
