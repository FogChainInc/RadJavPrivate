=== tests/cases/conformance/statements/tryStatements/tryStatements.jump ===
function fn() {
>fn : Symbol(fn, Decl(tryStatements.jump, 0, 0))

    try { } catch { }

    try { } catch { 
        try { } catch { 
            try { } catch { }
        }
        try { } catch { }
    }

    try { } catch (x) { var x: any; }
>x : Symbol(x, Decl(tryStatements.jump, 10, 19))
>x : Symbol(x, Decl(tryStatements.jump, 10, 27))

    try { } finally { }

    try { } catch { } finally { }

    try { } catch (z) { } finally { }
>z : Symbol(z, Decl(tryStatements.jump, 16, 19))
}
