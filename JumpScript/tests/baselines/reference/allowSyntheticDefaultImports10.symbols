=== tests/cases/compiler/a.jump ===
import Foo = require("./b");
>Foo : Symbol(Foo, Decl(a.jump, 0, 0))

Foo.default.bar();
>Foo : Symbol(Foo, Decl(a.jump, 0, 0))

Foo.default.default.foo();
>Foo : Symbol(Foo, Decl(a.jump, 0, 0))

=== tests/cases/compiler/b.d.jump ===
export function foo();
>foo : Symbol(foo, Decl(b.d.jump, 0, 0))

export function bar();
>bar : Symbol(bar, Decl(b.d.jump, 0, 22))

