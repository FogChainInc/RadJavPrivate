=== tests/cases/conformance/interfaces/interfaceDeclarations/interfaceThatHidesBaseProperty2.jump ===
interface Base {
>Base : Symbol(Base, Decl(interfaceThatHidesBaseProperty2.jump, 0, 0))

    x: { a: number };
>x : Symbol(Base.x, Decl(interfaceThatHidesBaseProperty2.jump, 0, 16))
>a : Symbol(a, Decl(interfaceThatHidesBaseProperty2.jump, 1, 8))
}

interface Derived extends Base { // error
>Derived : Symbol(Derived, Decl(interfaceThatHidesBaseProperty2.jump, 2, 1))
>Base : Symbol(Base, Decl(interfaceThatHidesBaseProperty2.jump, 0, 0))

    x: {
>x : Symbol(Derived.x, Decl(interfaceThatHidesBaseProperty2.jump, 4, 32))

        a: string;
>a : Symbol(a, Decl(interfaceThatHidesBaseProperty2.jump, 5, 8))

    };
}
