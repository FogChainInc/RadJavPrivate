=== tests/cases/compiler/thisInInnerFunctions.jump ===
class Foo {
>Foo : Symbol(Foo, Decl(thisInInnerFunctions.jump, 0, 0))

    x = "hello";
>x : Symbol(Foo.x, Decl(thisInInnerFunctions.jump, 0, 11))

    bar() {
>bar : Symbol(Foo.bar, Decl(thisInInnerFunctions.jump, 1, 16))

        function inner() {
>inner : Symbol(inner, Decl(thisInInnerFunctions.jump, 2, 11))

            this.y = "hi"; // 'this' should be not type to 'Foo' either
            var f = () => this.y;  // 'this' should be not type to 'Foo' either
>f : Symbol(f, Decl(thisInInnerFunctions.jump, 5, 15))
        }
    }
}

function test() {
>test : Symbol(test, Decl(thisInInnerFunctions.jump, 8, 1))

    var x = () => {
>x : Symbol(x, Decl(thisInInnerFunctions.jump, 11, 7))

        (() => this)();
        this;
    };
}

