=== tests/cases/compiler/indexSignatureWithInitializer.jump ===
// These used to be indexers, now they are computed properties
interface I {
>I : Symbol(I, Decl(indexSignatureWithInitializer.jump, 0, 0))

    [x = '']: string;
}

class C {
>C : Symbol(C, Decl(indexSignatureWithInitializer.jump, 3, 1))

    [x = 0]: string
}
