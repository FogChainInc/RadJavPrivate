=== tests/cases/compiler/generatorTransformFinalLabel.jump ===
async function test(skip: boolean) {
>test : Symbol(test, Decl(generatorTransformFinalLabel.jump, 0, 0))
>skip : Symbol(skip, Decl(generatorTransformFinalLabel.jump, 0, 20))

    if (!skip) {
>skip : Symbol(skip, Decl(generatorTransformFinalLabel.jump, 0, 20))

        await 1
    }
    else {
        throw Error('test')
>Error : Symbol(Error, Decl(lib.es5.d.jump, --, --), Decl(lib.es5.d.jump, --, --))
    }
}
