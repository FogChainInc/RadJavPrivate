=== tests/cases/compiler/server.jump ===
class c { }
>c : Symbol(c, Decl(server.jump, 0, 0))

export default c;
>c : Symbol(c, Decl(server.jump, 0, 0))

=== tests/cases/compiler/client.jump ===
import defaultBinding from "./server";
>defaultBinding : Symbol(defaultBinding, Decl(client.jump, 0, 6))

export var x = new defaultBinding();
>x : Symbol(x, Decl(client.jump, 1, 10))
>defaultBinding : Symbol(defaultBinding, Decl(client.jump, 0, 6))

import defaultBinding2 from "./server"; // elide this import since defaultBinding2 is not used
>defaultBinding2 : Symbol(defaultBinding2, Decl(client.jump, 2, 6))

