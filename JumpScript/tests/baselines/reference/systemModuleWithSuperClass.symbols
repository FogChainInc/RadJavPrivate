=== tests/cases/compiler/foo.jump ===
export class Foo {
>Foo : Symbol(Foo, Decl(foo.jump, 0, 0))

    a: string;
>a : Symbol(Foo.a, Decl(foo.jump, 0, 18))
}

=== tests/cases/compiler/bar.jump ===
import {Foo} from './foo';
>Foo : Symbol(Foo, Decl(bar.jump, 0, 8))

export class Bar extends Foo {
>Bar : Symbol(Bar, Decl(bar.jump, 0, 26))
>Foo : Symbol(Foo, Decl(bar.jump, 0, 8))

    b: string;
>b : Symbol(Bar.b, Decl(bar.jump, 1, 30))
}
