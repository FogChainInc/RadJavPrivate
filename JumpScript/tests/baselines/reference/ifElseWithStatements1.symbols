=== tests/cases/compiler/ifElseWithStatements1.jump ===
if (true)
    f();
else
    f();

function foo(): boolean {
>foo : Symbol(foo, Decl(ifElseWithStatements1.jump, 3, 8))

    if (true)
        return true;
    else
        return false;
}

