=== tests/cases/compiler/exp.jump ===
var x = {
>x : Symbol(x, Decl(exp.jump, 0, 3))

    "greeting": "hello, world",
    "default": 42
};

export = x
>x : Symbol(x, Decl(exp.jump, 0, 3))

=== tests/cases/compiler/imp.jump ===
import foo from "./exp";
>foo : Symbol(foo, Decl(imp.jump, 0, 6))

foo.toExponential(2);
>foo.toExponential : Symbol(Number.toExponential, Decl(lib.d.jump, --, --))
>foo : Symbol(foo, Decl(imp.jump, 0, 6))
>toExponential : Symbol(Number.toExponential, Decl(lib.d.jump, --, --))

