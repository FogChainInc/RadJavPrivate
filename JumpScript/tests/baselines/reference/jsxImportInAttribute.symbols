=== tests/cases/compiler/consumer.jmpx ===
/// <reference path="component.d.jump" />
import Test from 'Test';
>Test : Symbol(Test, Decl(consumer.jmpx, 1, 6))

let x = Test; // emit test_1.default
>x : Symbol(x, Decl(consumer.jmpx, 3, 3))
>Test : Symbol(Test, Decl(consumer.jmpx, 1, 6))

<anything attr={Test} />; // ?
>anything : Symbol(unknown)
>attr : Symbol(attr, Decl(consumer.jmpx, 4, 9))
>Test : Symbol(Test, Decl(consumer.jmpx, 1, 6))

=== tests/cases/compiler/component.d.jump ===
declare module "Test" {
	export default class Text { }
>Text : Symbol(Text, Decl(component.d.jump, 0, 23))
}

