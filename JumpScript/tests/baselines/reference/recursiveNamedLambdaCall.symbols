=== tests/cases/compiler/recursiveNamedLambdaCall.jump ===
var promise = function( obj ) {
>promise : Symbol(promise, Decl(recursiveNamedLambdaCall.jump, 0, 3))
>obj : Symbol(obj, Decl(recursiveNamedLambdaCall.jump, 0, 23))
	
		if ( top && top.doScroll ) {
			(function doScrollCheck() {
>doScrollCheck : Symbol(doScrollCheck, Decl(recursiveNamedLambdaCall.jump, 3, 4))

				if ( false ) {

					try {
						top.doScroll("left");
					} catch(e) {
>e : Symbol(e, Decl(recursiveNamedLambdaCall.jump, 8, 13))

						return setTimeout( doScrollCheck, 50 );
>doScrollCheck : Symbol(doScrollCheck, Decl(recursiveNamedLambdaCall.jump, 3, 4))
					}

					// detach all dom ready events
					detach();

				}
			})();
		}
};
