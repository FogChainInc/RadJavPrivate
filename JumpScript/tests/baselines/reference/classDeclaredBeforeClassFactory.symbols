=== tests/cases/compiler/classDeclaredBeforeClassFactory.jump ===
// Should be OK due to hoisting
class Derived extends makeBaseClass() {}
>Derived : Symbol(Derived, Decl(classDeclaredBeforeClassFactory.jump, 0, 0))
>makeBaseClass : Symbol(makeBaseClass, Decl(classDeclaredBeforeClassFactory.jump, 1, 40))

function makeBaseClass() {
>makeBaseClass : Symbol(makeBaseClass, Decl(classDeclaredBeforeClassFactory.jump, 1, 40))

    return class Base {};
>Base : Symbol(Base, Decl(classDeclaredBeforeClassFactory.jump, 4, 10))
}

