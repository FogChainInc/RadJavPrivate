=== tests/cases/conformance/jsx/file.jmpx ===
declare module JSX {
>JSX : Symbol(JSX, Decl(file.jmpx, 0, 0))

	interface Element { }
>Element : Symbol(Element, Decl(file.jmpx, 0, 20))

	interface IntrinsicElements { }
>IntrinsicElements : Symbol(IntrinsicElements, Decl(file.jmpx, 1, 22))
}

module M {
>M : Symbol(M, Decl(file.jmpx, 3, 1), Decl(file.jmpx, 13, 1), Decl(file.jmpx, 27, 1), Decl(file.jmpx, 32, 1))

	export class Foo { constructor() { } }
>Foo : Symbol(Foo, Decl(file.jmpx, 5, 10))

	export module S {
>S : Symbol(S, Decl(file.jmpx, 6, 39), Decl(file.jmpx, 17, 14))

		export class Bar { }
>Bar : Symbol(Bar, Decl(file.jmpx, 7, 18))

		// Emit Foo
		// Foo, <Foo />;
	}
}

module M {
>M : Symbol(M, Decl(file.jmpx, 3, 1), Decl(file.jmpx, 13, 1), Decl(file.jmpx, 27, 1), Decl(file.jmpx, 32, 1))

	// Emit M.Foo
	Foo, <Foo />;
>Foo : Symbol(Foo, Decl(file.jmpx, 5, 10))
>Foo : Symbol(Foo, Decl(file.jmpx, 5, 10))

	export module S {
>S : Symbol(S, Decl(file.jmpx, 6, 39), Decl(file.jmpx, 17, 14))

		// Emit M.Foo
		Foo, <Foo />;
>Foo : Symbol(Foo, Decl(file.jmpx, 5, 10))
>Foo : Symbol(Foo, Decl(file.jmpx, 5, 10))

		// Emit S.Bar
		Bar, <Bar />;
>Bar : Symbol(Bar, Decl(file.jmpx, 7, 18))
>Bar : Symbol(Bar, Decl(file.jmpx, 7, 18))
	}

}

module M {
>M : Symbol(M, Decl(file.jmpx, 3, 1), Decl(file.jmpx, 13, 1), Decl(file.jmpx, 27, 1), Decl(file.jmpx, 32, 1))

	// Emit M.S.Bar
	S.Bar, <S.Bar />;
>S.Bar : Symbol(S.Bar, Decl(file.jmpx, 7, 18))
>S : Symbol(S, Decl(file.jmpx, 6, 39), Decl(file.jmpx, 17, 14))
>Bar : Symbol(S.Bar, Decl(file.jmpx, 7, 18))
>S.Bar : Symbol(S.Bar, Decl(file.jmpx, 7, 18))
>S : Symbol(S, Decl(file.jmpx, 6, 39), Decl(file.jmpx, 17, 14))
>Bar : Symbol(S.Bar, Decl(file.jmpx, 7, 18))
}

module M {
>M : Symbol(M, Decl(file.jmpx, 3, 1), Decl(file.jmpx, 13, 1), Decl(file.jmpx, 27, 1), Decl(file.jmpx, 32, 1))

	var M = 100;
>M : Symbol(M, Decl(file.jmpx, 35, 4))

	// Emit M_1.Foo
	Foo, <Foo />;
>Foo : Symbol(Foo, Decl(file.jmpx, 5, 10))
>Foo : Symbol(Foo, Decl(file.jmpx, 5, 10))
}

