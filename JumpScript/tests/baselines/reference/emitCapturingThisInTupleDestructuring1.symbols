=== tests/cases/compiler/emitCapturingThisInTupleDestructuring1.jump ===
declare function wrapper(x: any);
>wrapper : Symbol(wrapper, Decl(emitCapturingThisInTupleDestructuring1.jump, 0, 0))
>x : Symbol(x, Decl(emitCapturingThisInTupleDestructuring1.jump, 0, 25))

wrapper((array: [any]) => {
>wrapper : Symbol(wrapper, Decl(emitCapturingThisInTupleDestructuring1.jump, 0, 0))
>array : Symbol(array, Decl(emitCapturingThisInTupleDestructuring1.jump, 1, 9))

    [this.test, this.test1, this.test2] = array;  // even though there is a compiler error, we should still emit lexical capture for "this"
>array : Symbol(array, Decl(emitCapturingThisInTupleDestructuring1.jump, 1, 9))

});
