=== tests/cases/compiler/letKeepNamesOfTopLevelItems.jump ===
let x;
>x : Symbol(x, Decl(letKeepNamesOfTopLevelItems.jump, 0, 3))

function foo() {
>foo : Symbol(foo, Decl(letKeepNamesOfTopLevelItems.jump, 0, 6))

    let x;
>x : Symbol(x, Decl(letKeepNamesOfTopLevelItems.jump, 2, 7))
}

module A {
>A : Symbol(A, Decl(letKeepNamesOfTopLevelItems.jump, 3, 1))

    let x;
>x : Symbol(x, Decl(letKeepNamesOfTopLevelItems.jump, 6, 7))
}
