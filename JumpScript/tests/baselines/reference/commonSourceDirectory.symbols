=== /app/index.jump ===
/// <reference path="../types/bar.d.jump"/>
import { x } from "foo";
>x : Symbol(x, Decl(index.jump, 1, 8))

import { y } from "bar";
>y : Symbol(y, Decl(index.jump, 2, 8))

x + y;
>x : Symbol(x, Decl(index.jump, 1, 8))
>y : Symbol(y, Decl(index.jump, 2, 8))

=== /node_modules/foo/index.jump ===
// Test that importing a file from `node_modules` does not affect calculation of the common source directory.

export const x = 0;
>x : Symbol(x, Decl(index.jump, 2, 12))

=== /types/bar.d.jump ===
declare module "bar" {
    export const y = 0;
>y : Symbol(y, Decl(bar.d.jump, 1, 16))
}

