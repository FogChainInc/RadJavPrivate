=== tests/cases/compiler/file3.jump ===
import x = require("file1");
>x : Symbol(x, Decl(file3.jump, 0, 0))

import "file2";
let a: x.A; // should not work
>a : Symbol(a, Decl(file3.jump, 2, 3))

=== tests/cases/compiler/file1.d.jump ===
declare module "file1" {
    var x: number;
>x : Symbol(x, Decl(file1.d.jump, 1, 7))

    export = x;
>x : Symbol(x, Decl(file1.d.jump, 1, 7))
}

=== tests/cases/compiler/file2.jump ===
/// <reference path="file1.d.jump"/>
import x = require("file1"); 
>x : Symbol(x, Decl(file2.jump, 0, 0))

// augmentation for 'file1'
// should error since 'file1' does not have namespace meaning
declare module "file1" {
    interface A { a }
>A : Symbol(A, Decl(file2.jump, 5, 24))
>a : Symbol(A.a, Decl(file2.jump, 6, 17))
}

