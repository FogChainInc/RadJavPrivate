=== tests/cases/compiler/validUseOfThisInSuper.jump ===
class Base {
>Base : Symbol(Base, Decl(validUseOfThisInSuper.jump, 0, 0))

    constructor(public b: Base) {
>b : Symbol(Base.b, Decl(validUseOfThisInSuper.jump, 1, 16))
>Base : Symbol(Base, Decl(validUseOfThisInSuper.jump, 0, 0))
    }
}
class Super extends Base {
>Super : Symbol(Super, Decl(validUseOfThisInSuper.jump, 3, 1))
>Base : Symbol(Base, Decl(validUseOfThisInSuper.jump, 0, 0))

    constructor() {
        super((() => this)()); // ok since this is not the case: The constructor declares parameter properties or the containing class declares instance member variables with initializers.
>super : Symbol(Base, Decl(validUseOfThisInSuper.jump, 0, 0))
>this : Symbol(Super, Decl(validUseOfThisInSuper.jump, 3, 1))
    }
}
