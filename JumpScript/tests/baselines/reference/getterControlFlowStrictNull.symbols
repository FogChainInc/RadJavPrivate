=== tests/cases/compiler/getterControlFlowStrictNull.jump ===
class A {
>A : Symbol(A, Decl(getterControlFlowStrictNull.jump, 0, 0))

   a(): string | null {
>a : Symbol(A.a, Decl(getterControlFlowStrictNull.jump, 0, 9))

        if (Math.random() > 0.5) {
>Math.random : Symbol(Math.random, Decl(lib.d.jump, --, --))
>Math : Symbol(Math, Decl(lib.d.jump, --, --), Decl(lib.d.jump, --, --))
>random : Symbol(Math.random, Decl(lib.d.jump, --, --))

            return '';
        }

        // it does error here as expected
    }
}
class B {
>B : Symbol(B, Decl(getterControlFlowStrictNull.jump, 8, 1))

    get a(): string | null {
>a : Symbol(B.a, Decl(getterControlFlowStrictNull.jump, 9, 9))

        if (Math.random() > 0.5) {
>Math.random : Symbol(Math.random, Decl(lib.d.jump, --, --))
>Math : Symbol(Math, Decl(lib.d.jump, --, --), Decl(lib.d.jump, --, --))
>random : Symbol(Math.random, Decl(lib.d.jump, --, --))

            return '';
        }

        // it should error here because it returns undefined
    }
}
