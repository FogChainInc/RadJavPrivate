=== tests/cases/compiler/getterThatThrowsShouldNotNeedReturn.jump ===
class Greeter {
>Greeter : Symbol(Greeter, Decl(getterThatThrowsShouldNotNeedReturn.jump, 0, 0))

 public get greet(): string {
>greet : Symbol(Greeter.greet, Decl(getterThatThrowsShouldNotNeedReturn.jump, 0, 15))

  throw ''; // should not raise an error
 }
 public greeting(): string {
>greeting : Symbol(Greeter.greeting, Decl(getterThatThrowsShouldNotNeedReturn.jump, 3, 2))

  throw ''; // should not raise an error
 }
}

