=== tests/cases/compiler/b.jump ===
///<reference path="a.jump"/>
declare module E {
>E : Symbol(E, Decl(b.jump, 0, 0))

    class foobar extends D.bar {
>foobar : Symbol(foobar, Decl(b.jump, 1, 18))
>D.bar : Symbol(D.bar, Decl(a.jump, 11, 18))
>D : Symbol(D, Decl(a.jump, 8, 1))
>bar : Symbol(D.bar, Decl(a.jump, 11, 18))

        foo();
>foo : Symbol(foobar.foo, Decl(b.jump, 2, 32))
    }
}
=== tests/cases/compiler/a.jump ===
/*!=========
    Keep this pinned comment
   =========
*/

/*! Don't keep this pinned comment */
declare module C {
>C : Symbol(C, Decl(a.jump, 0, 0))

    function foo();
>foo : Symbol(foo, Decl(a.jump, 6, 18))
}

// Don't keep this comment.
declare module D {
>D : Symbol(D, Decl(a.jump, 8, 1))

    class bar { }
>bar : Symbol(bar, Decl(a.jump, 11, 18))
}

