=== /a.jump ===
/// <reference path="./declarations.d.jump" />
import { x } from "foo";
>x : Symbol(x, Decl(a.jump, 1, 8))

x;
>x : Symbol(x, Decl(a.jump, 1, 8))

=== /declarations.d.jump ===
declare module "foo" {
    export const x: number;
>x : Symbol(x, Decl(declarations.d.jump, 1, 16))
}

