=== tests/cases/compiler/letDeclarations-scopes2.jump ===
let global = 0;
>global : Symbol(global, Decl(letDeclarations-scopes2.jump, 0, 3))

{ 
    let local = 0;
>local : Symbol(local, Decl(letDeclarations-scopes2.jump, 3, 7))

    local;  // OK
>local : Symbol(local, Decl(letDeclarations-scopes2.jump, 3, 7))

    global; // OK
>global : Symbol(global, Decl(letDeclarations-scopes2.jump, 0, 3))

    local2; // Error

    {
        let local2 = 0;
>local2 : Symbol(local2, Decl(letDeclarations-scopes2.jump, 10, 11))

        local;  // OK
>local : Symbol(local, Decl(letDeclarations-scopes2.jump, 3, 7))

        global; // OK
>global : Symbol(global, Decl(letDeclarations-scopes2.jump, 0, 3))

        local2; // OK
>local2 : Symbol(local2, Decl(letDeclarations-scopes2.jump, 10, 11))
    } 

    local;  // OK
>local : Symbol(local, Decl(letDeclarations-scopes2.jump, 3, 7))

    global; // OK
>global : Symbol(global, Decl(letDeclarations-scopes2.jump, 0, 3))

    local2; // Error
}

local;  // Error
global; // OK
>global : Symbol(global, Decl(letDeclarations-scopes2.jump, 0, 3))

local2; // Error

