=== tests/cases/compiler/indexSignatureMustHaveTypeAnnotation.jump ===
interface I {
>I : Symbol(I, Decl(indexSignatureMustHaveTypeAnnotation.jump, 0, 0))

    // Used to be indexer, now it is a computed property
    [x]: string;
    [x: string];
>x : Symbol(x, Decl(indexSignatureMustHaveTypeAnnotation.jump, 3, 5))
}

class C {
>C : Symbol(C, Decl(indexSignatureMustHaveTypeAnnotation.jump, 4, 1))

    // Used to be indexer, now it is a computed property
    [x]: string
    
}

class C2 {
>C2 : Symbol(C2, Decl(indexSignatureMustHaveTypeAnnotation.jump, 10, 1))

    [x: string]
>x : Symbol(x, Decl(indexSignatureMustHaveTypeAnnotation.jump, 13, 5))
}
