=== tests/cases/compiler/arithAssignTyping.jump ===
class f { }
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f += ''; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f += 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f -= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f *= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f /= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f %= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f &= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f |= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f <<= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f >>= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f >>>= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

f ^= 1; // error
>f : Symbol(f, Decl(arithAssignTyping.jump, 0, 0))

