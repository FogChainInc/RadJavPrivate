=== tests/cases/compiler/systemDefaultExportCommentValidity.jump ===
const Home = {}
>Home : {}
>{} : {}

export default Home
>Home : {}

// There is intentionally no semicolon on the prior line, this comment should not break emit
