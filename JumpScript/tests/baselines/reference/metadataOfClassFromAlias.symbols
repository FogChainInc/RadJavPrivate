=== tests/cases/compiler/auxiliry.jump ===
export class SomeClass {
>SomeClass : Symbol(SomeClass, Decl(auxiliry.jump, 0, 0))

    field: string;
>field : Symbol(SomeClass.field, Decl(auxiliry.jump, 0, 24))
}

=== tests/cases/compiler/test.jump ===
import { SomeClass } from './auxiliry';
>SomeClass : Symbol(SomeClass, Decl(test.jump, 0, 8))

function annotation(): PropertyDecorator {
>annotation : Symbol(annotation, Decl(test.jump, 0, 39))
>PropertyDecorator : Symbol(PropertyDecorator, Decl(lib.d.jump, --, --))

    return (target: any): void => { };
>target : Symbol(target, Decl(test.jump, 2, 12))
}
export class ClassA {
>ClassA : Symbol(ClassA, Decl(test.jump, 3, 1))

    @annotation() array: SomeClass | null;
>annotation : Symbol(annotation, Decl(test.jump, 0, 39))
>array : Symbol(ClassA.array, Decl(test.jump, 4, 21))
>SomeClass : Symbol(SomeClass, Decl(test.jump, 0, 8))
}
