=== tests/cases/compiler/ambientGetters.jump ===
declare class A {
>A : Symbol(A, Decl(ambientGetters.jump, 0, 0))

    get length() : number;
>length : Symbol(A.length, Decl(ambientGetters.jump, 0, 17))
}

declare class B {
>B : Symbol(B, Decl(ambientGetters.jump, 2, 1))

    get length() { return 0; }
>length : Symbol(B.length, Decl(ambientGetters.jump, 4, 17))
}
