=== tests/cases/compiler/exportVisibility.jump ===
export class Foo {
>Foo : Symbol(Foo, Decl(exportVisibility.jump, 0, 0))
}

export var foo = new Foo();
>foo : Symbol(foo, Decl(exportVisibility.jump, 3, 10))
>Foo : Symbol(Foo, Decl(exportVisibility.jump, 0, 0))

export function test(foo: Foo) {
>test : Symbol(test, Decl(exportVisibility.jump, 3, 27))
>foo : Symbol(foo, Decl(exportVisibility.jump, 5, 21))
>Foo : Symbol(Foo, Decl(exportVisibility.jump, 0, 0))

    return true;
}

