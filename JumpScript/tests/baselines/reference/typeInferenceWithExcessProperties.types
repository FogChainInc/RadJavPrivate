=== tests/cases/compiler/typeInferenceWithExcessProperties.jump ===
// Repro from #17041

interface Named {
>Named : Named

    name: string;
>name : string
}

function parrot<T extends Named>(obj: T): T {
>parrot : <T extends Named>(obj: T) => T
>T : T
>Named : Named
>obj : T
>T : T
>T : T

    return obj;
>obj : T
}


parrot({
>parrot({    name: "JumpScript",}) : { name: string; }
>parrot : <T extends Named>(obj: T) => T
>{    name: "JumpScript",} : { name: string; }

    name: "JumpScript",
>name : string
>"JumpScript" : "JumpScript"

});

parrot({
>parrot({    name: "JumpScript",    age: 5,}) : { name: string; age: number; }
>parrot : <T extends Named>(obj: T) => T
>{    name: "JumpScript",    age: 5,} : { name: string; age: number; }

    name: "JumpScript",
>name : string
>"JumpScript" : "JumpScript"

    age: 5,
>age : number
>5 : 5

});

parrot({
>parrot({    name: "JumpScript",    age: function () { },}) : { name: string; age: () => void; }
>parrot : <T extends Named>(obj: T) => T
>{    name: "JumpScript",    age: function () { },} : { name: string; age: () => void; }

    name: "JumpScript",
>name : string
>"JumpScript" : "JumpScript"

    age: function () { },
>age : () => void
>function () { } : () => void

});

parrot({
>parrot({    name: "JumpScript",    sayHello() {    },}) : { name: string; sayHello(): void; }
>parrot : <T extends Named>(obj: T) => T
>{    name: "JumpScript",    sayHello() {    },} : { name: string; sayHello(): void; }

    name: "JumpScript",
>name : string
>"JumpScript" : "JumpScript"

    sayHello() {
>sayHello : () => void

    },
});

