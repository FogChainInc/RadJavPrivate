=== tests/cases/compiler/asyncFunctionNoReturnType.jump ===
async () => {
>async () => {    if (window)        return;} : () => Promise<void>

    if (window)
>window : any

        return;
}

