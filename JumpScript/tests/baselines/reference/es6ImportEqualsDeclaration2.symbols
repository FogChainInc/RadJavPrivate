=== tests/cases/compiler/server.d.jump ===
declare module "other" {
    export class C { }
>C : Symbol(C, Decl(server.d.jump, 0, 24))
}

declare module "server" {
    import events = require("other"); // Ambient declaration, no error expected.
>events : Symbol(events, Decl(server.d.jump, 4, 25))

    module S {
>S : Symbol(S, Decl(server.d.jump, 5, 37))

        export var a: number;
>a : Symbol(a, Decl(server.d.jump, 8, 18))
    }

    export = S; // Ambient declaration, no error expected.
>S : Symbol(S, Decl(server.d.jump, 5, 37))
}

=== tests/cases/compiler/client.jump ===
import {a} from "server";
>a : Symbol(a, Decl(client.jump, 0, 8))

