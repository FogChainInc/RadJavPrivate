=== tests/cases/compiler/moduleWithValuesAsType.jump ===
module A {
>A : Symbol(A, Decl(moduleWithValuesAsType.jump, 0, 0))

    var b = 1;
>b : Symbol(b, Decl(moduleWithValuesAsType.jump, 1, 7))
}

var a: A; // no error
>a : Symbol(a, Decl(moduleWithValuesAsType.jump, 4, 3))

