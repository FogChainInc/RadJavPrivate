=== tests/cases/compiler/consumer.jump ===
import x = require('./foo');
>x : Symbol(x, Decl(consumer.jump, 0, 0))

x.X // .jump should be picked
>x.X : Symbol(x.X, Decl(foo.jump, 1, 14))
>x : Symbol(x, Decl(consumer.jump, 0, 0))
>X : Symbol(x.X, Decl(foo.jump, 1, 14))

=== tests/cases/compiler/foo.jump ===
module M2 {
>M2 : Symbol(M2, Decl(foo.jump, 0, 0))

    export var X = 1;
>X : Symbol(X, Decl(foo.jump, 1, 14))
}
export = M2
>M2 : Symbol(M2, Decl(foo.jump, 0, 0))

