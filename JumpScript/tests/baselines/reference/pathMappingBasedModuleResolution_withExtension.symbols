=== /a.jump ===
import { foo } from "foo";
>foo : Symbol(foo, Decl(a.jump, 0, 8))

import { bar } from "bar";
>bar : Symbol(bar, Decl(a.jump, 1, 8))

=== /foo/foo.jump ===
export function foo() {}
>foo : Symbol(foo, Decl(foo.jump, 0, 0))

=== /bar/bar.js ===
export function bar() {}
>bar : Symbol(bar, Decl(bar.js, 0, 0))

