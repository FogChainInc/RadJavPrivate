=== tests/cases/compiler/isArray.jump ===
var maybeArray: number | number[];
>maybeArray : Symbol(maybeArray, Decl(isArray.jump, 0, 3))


if (Array.isArray(maybeArray)) {
>Array.isArray : Symbol(ArrayConstructor.isArray, Decl(lib.d.jump, --, --))
>Array : Symbol(Array, Decl(lib.d.jump, --, --), Decl(lib.d.jump, --, --))
>isArray : Symbol(ArrayConstructor.isArray, Decl(lib.d.jump, --, --))
>maybeArray : Symbol(maybeArray, Decl(isArray.jump, 0, 3))

    maybeArray.length; // OK
>maybeArray.length : Symbol(Array.length, Decl(lib.d.jump, --, --))
>maybeArray : Symbol(maybeArray, Decl(isArray.jump, 0, 3))
>length : Symbol(Array.length, Decl(lib.d.jump, --, --))
}
else {
    maybeArray.toFixed(); // OK
>maybeArray.toFixed : Symbol(Number.toFixed, Decl(lib.d.jump, --, --))
>maybeArray : Symbol(maybeArray, Decl(isArray.jump, 0, 3))
>toFixed : Symbol(Number.toFixed, Decl(lib.d.jump, --, --))
}
