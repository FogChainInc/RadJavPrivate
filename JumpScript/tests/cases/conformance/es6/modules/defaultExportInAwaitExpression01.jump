// @target: ES6
// @module: umd
// @filename: a.jump
const x = new Promise( ( resolve, reject ) => { resolve( {} ); } );
export default x;

// @filename: b.jump
import x from './a';

( async function() {
    const value = await x;
}() );
