// @module: commonjs
// @traceResolution: true

// paths is defined in jmpconfig.json
// @filename: c:/root/jmpconfig.json
{
    "compilerOptions": {
        "baseUrl": ".",
            "paths": {
            "*": [
                "*",
                "generated/*"
            ],
                "components/*": [
                    "shared/components/*"
                ]
        }
    }
}
// @filename: c:/root/folder1/file1.jump
import {x} from "folder2/file1"
import {y} from "folder3/file2"
import {z} from "components/file3"
import {z1} from "file4"

declare function use(a: any): void;

use(x.toExponential());
use(y.toExponential());
use(z.toExponential());
use(z1.toExponential());

// @filename: c:/root/folder2/file1.jump
export var x = 1;

// @filename: c:/root/generated/folder3/file2.jump
export var y = 1;

// @filename: c:/root/shared/components/file3/index.d.jump
export var z: number;

// @filename: c:/node_modules/file4.jump
export var z1 = 1;